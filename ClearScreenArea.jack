class ClearScreenArea{
    function void ClearScreenArea(RigidBody a){
            do Screen.setColor(false);
            do Screen.drawRectangle(a.getTopLeftX, a.getTopLeftY, a.getBottomRightX, a.getBottomRightY);
    }
}